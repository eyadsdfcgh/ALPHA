# 🎬 دليل تحويل الفيديوهات MKV إلى MP4
# ======================================

## المشكلة:
الفيديوهات 1.mkv و 2.mkv لا تعمل في المتصفح لأن معظم المتصفحات لا تدعم صيغة MKV بشكل native

## ✅ الحل: تحويل MKV → MP4

### الطريقة 1: استخدام FFmpeg (الأفضل - بدون فقدان جودة)

1. تحميل FFmpeg:
   - زيارة: https://ffmpeg.org/download.html
   - للويندوز: https://www.gyan.dev/ffmpeg/builds/
   - تحميل "ffmpeg-release-essentials.zip"
   - استخراج الملفات
   - إضافة مجلد bin إلى PATH

2. التحويل بدون فقدان جودة (نسخ الستريم مباشرة):
   
   cd C:\Users\sameh\Desktop\ALPHA\course
   
   ffmpeg -i 1.mkv -c:v copy -c:a copy 1_new.mp4
   ffmpeg -i 2.mkv -c:v copy -c:a copy 2_new.mp4
   
   # ثم استبدل الملفات القديمة:
   del 1.mkv
   del 2.mkv
   ren 1_new.mp4 1.mp4
   ren 2_new.mp4 2.mp4

3. إذا لم يعمل النسخ المباشر (حالات نادرة)، استخدم إعادة تشفير:
   
   ffmpeg -i 1.mkv -c:v libx264 -preset medium -crf 23 -c:a aac -b:a 128k 1.mp4
   ffmpeg -i 2.mkv -c:v libx264 -preset medium -crf 23 -c:a aac -b:a 128k 2.mp4

### الطريقة 2: استخدام HandBrake (واجهة رسومية سهلة)

1. تحميل HandBrake:
   - زيارة: https://handbrake.fr/downloads.php
   - تحميل النسخة للويندoز

2. الخطوات:
   - فتح HandBrake
   - سحب ملف 1.mkv إلى البرنامج
   - اختيار Preset: "Fast 1080p30" أو "H.265 MKV 1080p30"
   - تغيير Format من MKV إلى MP4
   - النقر على "Start Encode"
   - تكرار العملية مع 2.mkv

### الطريقة 3: استخدام VLC Media Player (موجود عند الجميع)

1. فتح VLC
2. Media → Convert / Save
3. Add الملف 1.mkv
4. Convert/Save
5. اختيار Profile: "Video - H.264 + MP3 (MP4)"
6. اختيار مكان الحفظ: 1_converted.mp4
7. Start

## 🔧 تعديل الكود بعد التحويل:

بعد تحويل الملفات، لا يوجد تعديل مطلوب! الكود الحالي سيعمل تلقائياً لأنه يتحقق من رقم الفيديو:
- الفيديوهات 1-2: سيبحث عن .mkv (لكن بعد التحويل سيصبح .mp4)

يُفضل تحديث الكود ليبحث عن mp4 فقط بعد التحويل الكامل.

## 📌 ملاحظات مهمة:

- **الطريقة الأسرع**: FFmpeg مع -c copy (ثواني معدودة)
- **أفضل جودة**: FFmpeg مع CRF 18-23
- **الأسهل للمبتدئين**: HandBrake
- **متوفر عند الجميع**: VLC

## 🛡️ الحماية من السرقة (تم إضافتها):

✅ Dynamic watermark بـ username + user ID
✅ منع التحميل المباشر
✅ Video streaming عبر Flask (بدلاً من الوصول المباشر للملفات)
✅ Session-based access (كل جلسة بـ token فريد)
✅ منع فتح أكثر من تاب واحد
✅ تعطيل Developer Tools
✅ منع Right-click
✅ منع لقطات الشاشة (جزئياً - محدود بسبب قيود المتصفح)
✅ Watermark متحرك يصعب إزالته
✅ تسجيل كل وصول للفيديوهات في Console
✅ منع Picture-in-Picture mode

## ⚠️ تحذير:

**لا يمكن منع السرقة بنسبة 100%** - أي محتوى يُعرض على الشاشة يمكن تسجيله بكاميرا خارجية
أو برامج تسجيل متقدمة. لكن الحماية الحالية تجعل السرقة:
- صعبة جداً للمستخدم العادي
- تترك أثراً (username + user ID على كل فيديو مسروق)
- تستهلك وقتاً وجهداً كبيراً
